// Pagina del checkout, cierre de compra una vez cuando haya introducido su direccion
import { CheckoutSection } from "@/components/checkout/checkout-section/CheckoutSection";

// Productos de prueba
// const productInCart = [
//   initialData.products[0],
//   initialData.products[1],
//   initialData.products[2],
// ];

// Se crea una carpeta con el mismo nombre de la carpeta contenedora
// checkout/(checkout)/page.tsx, con el fin de que la ruta sea checkout
export default function () {
  return <CheckoutSection />;
}
